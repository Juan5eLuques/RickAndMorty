export type Character = {
  id: number;
  name: string;
  image: string;
  gender: string;
  specie: string;
  status: string;
};

export type CharacterLocation = {
  name: string;
  locationInfoURL: string;
};
